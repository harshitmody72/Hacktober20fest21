using System;

public static class HelloWorld
{
    public static void Main(string[] args)
    {
        // Output to the console like so
        System.Console.Write("Hello, World!");

        // Prevents the console from closing until the user presses a key
        System.Console.Read();
    }
}